import React from 'react';
import Settings from './Settings.jsx';

// Wrapper to render Settings with employer-specific heading/context if needed later
export default function EmployerSettingsPage() {
  return (
    <div className="space-y-2">
      <div>
        <h1 className="text-3xl font-semibold">Employer Settings</h1>
        <p className="text-muted-foreground">Manage your company account preferences</p>
      </div>
      <Settings />
    </div>
  );
}
